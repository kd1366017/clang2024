#include <stdio.h>
main() {
	char s[21];//文字型変数宣言s[]の箱は0～20個
	int i, k[20];//整数型変数宣言
	printf("文字列を入力してください＞");
	scanf("%s", &s[0]);//&s[0]で'\0'まで
	for (i = 0; s[i] != '\0'; i++) {//s[i]の中身が'\0'になるまで繰り返し
		printf("s[%d]の複合化キー＞ ",i);
		scanf("%d", &k[i]);//複合化キーの入力
		s[i] = s[i] - k[i];
	}
	printf("複合化済み文字列は、%s", &s[0]);
}